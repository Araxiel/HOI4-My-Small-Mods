
economy_decisions = {
	app_extra_research_slot = {

		icon = GFX_decision_hol_exchange_intelligence_data

		priority = 10

		allowed = {
			OR = {
				original_tag = NOR
				original_tag = PER
				original_tag = ICE
				original_tag = CHL
				original_tag = AFG
				original_tag = ARG
				original_tag = BRA
			}
		}

		visible = {
			amount_research_slots = 4
			is_ai = no
		}

		available = {
			num_of_controlled_factories > 100
		}
		
        fire_only_once = yes
		
        days_remove = 120
		
        modifier = {
        }
		
		complete_effect = {
		}
		
        remove_effect = {
			add_research_slot = 1			
        }
		
        cancel_trigger = {
        }
		
        cancel_effect = {
        }
    }
	
	app_give_helicopter_mio = {

		icon = GFX_decision_hol_exchange_intelligence_data

		priority = 10

		allowed = {
			NOT = {
				OR = {
					original_tag = BEL
					original_tag = GER
					original_tag = JAP
					original_tag = USA
					original_tag = PRC # has an air company that includes helicopters
				}
			}
		}

		visible = {
			is_ai = no
			OR = {
				is_special_project_being_researched = sp:sp_air_helicopter
				is_special_project_completed = sp:sp_air_helicopter
			}
		}
		
		available = {
			is_special_project_completed = sp:sp_air_helicopter
		}
		
        fire_only_once = yes
		
        days_remove = 60
		
        modifier = {
        }
		
		complete_effect = {
		}
		
        remove_effect = {
			unlock_military_industrial_organization_tooltip = mio:app_generic_helicopter_organization
        }
		
        cancel_trigger = {
        }
		
        cancel_effect = {
        }
    }
	
	app_handout_goods = {
		
		icon = GFX_decision_hol_exchange_intelligence_data
		
		priority = 10
		
		allowed = {
		}
		
		days_remove = 120
		
		visible = {
		}
		
		available = {
			# has_stability < 1
		}
		
		modifier = {
			political_power_gain = 0.5
			consumer_goods_factor = 0.15
		}
		
		complete_effect = {
			add_political_power = 25
		}
		
		remove_effect = {
			add_political_power = 25
		}
		
		ai_will_do = {
			base = 0
		}
		
	}
	
}

foreign_influence = {
	app_integrate = {

		icon = eng_propaganda_campaigns

		days_remove = 120

		allowed = {
			NOT = { original_tag = ENG }
		}
		
		visible = {
			is_ai = no
			FROM = {
				is_puppet_of = ROOT
				has_government = ROOT
			}
			has_government = communism
		}
		
		available = {
			FROM = {
				is_puppet_of = ROOT
			}
			FROM = {
				communism > 0.7
			}
			OR = {
				political_power_daily > 0.3
				has_political_power > 40
			}
		}

		target_array = subjects

		target_trigger = {
			FROM = {
				is_puppet_of = ROOT
			}
		}

		complete_effect = {
			FROM = {
				add_timed_idea = { idea = app_integration days = 120 }
			}
		}
		modifier = {
			political_power_cost = 0.3
		}

		ai_will_do = {
			base = 0
		}
	}
}

foreign_politics = {
	
	CZE_hand_over_ukraine = {

		icon = GFX_decision_hol_exchange_intelligence_data

		priority = 10

		allowed = {
			original_tag = CZE
		}

		visible = {
			is_ai = no
			RUT = {
				exists = yes
				is_subject_of = CZE
			}
			UKR = {
				exists = yes
				is_subject = yes
			}
		}
		available = {
			UKR = {
				overlord = {
					is_in_faction_with = CZE
					has_opinion = {
						target = CZE
						value > 24
					}
				}
			}
		}
		
        fire_only_once = yes
		
        days_remove = 15
		
		remove_effect = {
			every_state = {
				limit = {
					is_core_of = UKR
				}
				add_core_of = RUT
			}
			RUT = {
				annex_country = {
					target = UKR
					transfer_troops = yes
				}
			}
		}
	}
	
	CZE_hand_over_ukranian_lwow = {

		icon = GFX_decision_hol_exchange_intelligence_data

		priority = 10

		allowed = {
			original_tag = CZE
		}

		visible = {
			is_ai = no
			OR = {
				NOT = { owns_state = 91 }
				any_subject_country = {
					NOT = { owns_state = 91 }
				}
			}
			OR = {
				RUT = {
					exists = yes
					is_subject_of = CZE
					owns_state = 89
				}
				ROOT = {
					owns_state = 73
					owns_state = 89
				}
			}
		}
		
		available = {
			91 = {
				owner = {
					controls_state = PREV
					is_in_faction_with = CZE
					has_opinion = {
						target = CZE
						value > 24
					}
				}
			}
		}
		
        fire_only_once = yes
		
        days_remove = 15
		
		remove_effect = {
			if = {
				limit = {
					RUT = {
						exists = yes
						is_subject_of = CZE
					}
				}
				RUT = {
					transfer_state = 91
				}
			}
			else = {
				transfer_state = 91
			}
		}
	}
	
	## Gotterfixes
	
	GER_gf_negotiate_finish_soviet_peace = {

		icon = GFX_decision_hol_exchange_intelligence_data

		priority = 10

		allowed = {
			original_tag = GER
		}

		visible = {
			FIN = {
				has_war_with = SOV
				NOT = { has_war_together_with = GER }
				OR = {
					has_government = communism
					has_government = democratic
				}
			}
			SOV = {
				has_war_with = FIN
				NOT = { has_war_together_with = GER }
			}
			GER = {
				has_government = communism
				NOT = { has_war_with = FIN }
				NOT = { has_war_with = SOV }
			}
			is_ai = no
		}

		available = {
			SOV = {
				has_opinion = {
					target = GER
					value > 25
				}
			}
			FIN = {
				surrender_progress < 0.2
			}
		}
		
        fire_only_once = yes
		
        days_remove = 30
			
        modifier = {
        }
		
        remove_effect = {
			FIN = {
				white_peace = {
					tag = SOV
				}
			}
			GER = { add_to_faction = FIN }
			SOV = {
				if = {
					limit = {
						controls_province = 147
					}
					transfer_state = 147
				}
				if = {
					limit = {
						controls_province = 146
					}
					transfer_state = 146
				}
				if = {
					limit = {
						controls_province = 722
					}
					transfer_state = 722
				}
			}
			146 = {
				set_demilitarized_zone = yes
			}
			news_event = { id = aat_news.120 hours = 12 }
        }
		
        cancel_trigger = {
			FIN = {
				OR = {
					has_capitulated = yes
					has_war = no
				}
			}
        }
		
        cancel_effect = {
        }
    }
	
	FRA_join_iberian_popular_front = {

		icon = GFX_decision_hol_exchange_intelligence_data

		priority = 10

		allowed = {
			original_tag = FRA
		}

		visible = {
			FRA = {
				NOT = { has_war_together_with = POR }
				NOT = { has_war_together_with = SPR }
				NOT = { has_war_together_with = SPD }
				has_government = communism
				NOT = {
					OR = {
						is_in_faction_with = POR
						is_in_faction_with = SPR
						is_in_faction_with = SPD
					}
					
				}
				OR = {
					POR = { has_government = communism }
					SPR = { has_government = communism }
					SPD = { has_government = communism }
				}
				is_in_faction = yes
			}
			is_ai = no
		}

		available = {
		}
		
        fire_only_once = yes
		
        days_remove = 25
		
        modifier = {
        }
		
		complete_effect = {
			every_country = {
				limit = {
					is_in_faction_with = FRA
				}
				set_country_flag = {
					flag = gf_merge_popular_front
					days = 60
					value = 1
				}
			}
			dismantle_faction = yes
			
		}
		
        remove_effect = {
			#every_country = {
			#	limit = {
			#		is_in_faction_with = POR
			#	}
			#	FRA = {
			#		add_to_faction = PREV
			#	}
			#}
			every_country = {
				limit = {
					has_country_flag = gf_merge_popular_front
				}
				POR = {
					add_to_faction = PREV
				}
			}
        }
		# FACTION_LEADER_TAG = { add_to_faction = TAG_TO_ADD }
		
        cancel_trigger = {
        }
		
        cancel_effect = {
        }
    }
	
	FRA_deepen_research_cooperation = {

		icon = GFX_decision_generic_research

		priority = 10

		allowed = {
			original_tag = FRA
		}

		visible = {
			FRA = {
				is_in_faction = yes
			}
			is_ai = no
		}

		available = {
		}
		
        fire_only_once = yes
		
        days_remove = 1
		
        modifier = {
        }
		
		complete_effect = {
			every_country = {
				limit = {
					is_in_faction_with = ROOT
					has_government = communism
				}
				add_to_tech_sharing_group = gf_popular_front_science_pool
			}
		}
		
        remove_effect = {

        }
		
        cancel_trigger = {
        }
		
        cancel_effect = {
        }
    }
	
	# FRA_incorporate_new_member = {
	# 	
	# 	icon = GFX_decision_generic_research
	# 	
	# 	priority = 10
	# 	
	# 	allowed = {
	# 		original_tag = FRA
	# 	}
	# 	
	# 	visible = {
	# 		FRA = {
	# 			is_in_faction = yes
	# 		}
	# 		is_ai = no
	# 	}
	# 	
	# 	available = {
	# 	}
	# 	
    #     fire_only_once = yes
	# 	
    #     days_remove = 1
	# 	
    #     modifier = {
    #     }
	# 	
	# 	target_trigger = {
	# 		FROM = {
	# 			has_government = communism
	# 			is_subject_of = GER
	# 			NOT = { has_autonomy_state = autonomy_volkskommissariat }
	# 		}
	# 	}
	# 	
	# 	complete_effect = {
	# 		every_country = {
	# 			limit = {
	# 				is_in_faction_with = ROOT
	# 				has_government = communism
	# 			}
	# 			add_to_tech_sharing_group = gf_popular_front_science_pool
	# 		}
	# 	}
	# 	
    #     remove_effect = {
	# 
    #     }
	# 	
    #     cancel_trigger = {
    #     }
	# 	
    #     cancel_effect = {
    #     }
    # }
	
	FIN_gf_demand_estonia_handover = {

		icon = GFX_decision_hol_exchange_intelligence_data

		priority = 10

		allowed = {
			original_tag = FIN
		}

		visible = {
			FIN = {
				NOT = { has_war_with = SOV }
				has_government = communism
				is_in_faction_with = SOV
				OR = {
					NOT = { owns_state = 191 }
					NOT = { owns_state = 813 }
					NOT = { owns_state = 812 }
					NOT = { owns_state = 811 }
					NOT = { owns_state = 13 }
				}
				has_completed_focus = FIN_finno_estonian_union
			}
			SOV = {
				has_government = communism
			}
			is_ai = no
		}

		available = {
			SOV = {
				has_opinion = {
					target = FIN
					value > 50
				}
			}
			FIN = {
				has_completed_focus = FIN_finno_estonian_union
				OR = {
					NOT = { owns_state = 191 }
					NOT = { owns_state = 813 }
					NOT = { owns_state = 812 }
					NOT = { owns_state = 811 }
					NOT = { owns_state = 13 }
				}
			}
		}
		
        # fire_only_once = yes
		
        # days_remove = 30
			
        modifier = {
        }
		
        complete_effect = {
			191 = {
				if = {
					limit = {
						is_owned_and_controlled_by = SOV
					}
					transfer_state_to = FIN
				}
			}
			813 = {
				if = {
					limit = {
						is_owned_and_controlled_by = SOV
					}
					transfer_state_to = FIN
				}
			}
			812 = {
				if = {
					limit = {
						is_owned_and_controlled_by = SOV
					}
					transfer_state_to = FIN
				}
			}
			811 = {
				if = {
					limit = {
						is_owned_and_controlled_by = SOV
					}
					transfer_state_to = FIN
				}
			}
			13 = {
				if = {
					limit = {
						is_owned_and_controlled_by = SOV
					}
					transfer_state_to = FIN
				}
			}
        }
		
    }
	
	FIN_gf_demand_baltic_puppet_handover = {

		icon = GFX_decision_hol_exchange_intelligence_data

		priority = 10

		allowed = {
			original_tag = FIN
		}

		visible = {
			NOT = { has_war_with = SOV }
			has_government = communism
			is_in_faction_with = SOV
			has_completed_focus = FIN_keepers_of_the_baltic_countries
			SOV = {
				has_government = communism
			}
			is_ai = no
			LIT = {
				exists = yes
				is_in_faction_with = SOV
				is_in_faction_with = FIN
				is_subject = yes
				NOT = { is_subject_of = FIN }
			}
		}

		available = {
			has_completed_focus = FIN_keepers_of_the_baltic_countries
			SOV = {
				has_opinion = {
					target = FIN
					value > 50
				}
			}
			LIT = {
				NOT = { is_subject_of = FIN }
			}
		}
		
        # fire_only_once = yes
		
        # days_remove = 30
			
        modifier = {
        }
		
        complete_effect = {
			annex_country = {
				target = LIT
				transfer_troops = yes
			}
        }
		
        cancel_trigger = {
        }
		
        cancel_effect = {
        }
    }
	
	FIN_gf_demand_nordics_handover = {

		icon = GFX_decision_hol_exchange_intelligence_data

		priority = 10

		allowed = {
			original_tag = FIN
		}

		visible = {
			has_completed_focus = FIN_keepers_of_the_baltic_countries
			NOT = { has_war_with = SOV }
			has_government = communism
			is_in_faction_with = SOV
			
			SOV = {
				has_government = communism
			}
			is_ai = no
			OR = {
				NOR = {
					exists = yes
					is_in_faction_with = SOV
					is_in_faction_with = FIN
					NOT = { is_subject_of = FIN }
				}
				DEN = {
					exists = yes
					is_in_faction_with = SOV
					is_in_faction_with = FIN
					NOT = { is_subject_of = FIN }
				}
				SWE = {
					exists = yes
					is_in_faction_with = SOV
					is_in_faction_with = FIN
					NOT = { is_subject_of = FIN }
				}
			}
		}

		available = {
			has_completed_focus = FIN_red_finland
			OR = {
				NOR = {
					exists = yes
					is_in_faction_with = SOV
					is_in_faction_with = FIN
					NOT = { is_subject_of = FIN }
				}
				DEN = {
					exists = yes
					is_in_faction_with = SOV
					is_in_faction_with = FIN
					NOT = { is_subject_of = FIN }
				}
				SWE = {
					exists = yes
					is_in_faction_with = SOV
					is_in_faction_with = FIN
					NOT = { is_subject_of = FIN }
				}
			}
		}
		
        # fire_only_once = yes
		
        # days_remove = 30
			
        modifier = {
        }
		
        complete_effect = {
			FIN = {
				if = {
					limit = {
						NOR = {
							exists = yes
							is_in_faction_with = SOV
							is_in_faction_with = FIN
							NOT = { is_subject_of = FIN }
						}
					}
					puppet = NOR
				}
				if = {
					limit = {
						DEN = {
							exists = yes
							is_in_faction_with = SOV
							is_in_faction_with = FIN
							NOT = { is_subject_of = FIN }
						}
					}
					puppet = DEN
				}
				if = {
					limit = {
						SWE = {
							exists = yes
							is_in_faction_with = SOV
							is_in_faction_with = FIN
							NOT = { is_subject_of = FIN }
						}
					}
					puppet = SWE
				}
			}
        }
		
        cancel_trigger = {
        }
		
        cancel_effect = {
        }
    }
	
	FIN_gf_nordics_integration = {

		icon = GFX_decision_hol_exchange_intelligence_data

		priority = 10

		allowed = {
			original_tag = FIN
		}

		visible = {
			is_ai = no
			# has_completed_focus = FIN_red_finland
			OR = {
				DEN = {
					is_subject_of = FIN
				}
				NOR = {
					is_subject_of = FIN
				}
				SWE = {
					is_subject_of = FIN
				}
			}
		}
		available = {
		}
		
        fire_only_once = yes
		
        days_remove = 120
		
		remove_effect = {
			if = {
				limit = {
					DEN = {
						exists = yes
						is_subject_of = FIN
					}
				}
				#transfer_units_fraction = {
				#	target = FIN
				#	stockpile_ratio = 0.9
				#	army_ratio = 1
				#	navy_ratio = 1
				#	air_ratio = 1
				#}
				FIN = {
					annex_country = {
						target = DEN
						transfer_troops = yes
					}
				}
			}
			if = {
				limit = {
					NOR = {
						exists = yes
						is_subject_of = FIN
					}
				}
				#transfer_units_fraction = {
				#	target = FIN
				#	stockpile_ratio = 0.9
				#	army_ratio = 1
				#	navy_ratio = 1
				#	air_ratio = 1
				#}
				FIN = {
					annex_country = {
						target = NOR
						transfer_troops = yes
					}
				}
			}
			if = {
				limit = {
					SWE = {
						exists = yes
						is_subject_of = FIN
					}
				}
				#transfer_units_fraction = {
				#	target = FIN
				#	stockpile_ratio = 0.9
				#	army_ratio = 1
				#	navy_ratio = 1
				#	air_ratio = 1
				#}
				FIN = {
					annex_country = {
						target = SWE
						transfer_troops = yes
					}
				}
			}
		}
	}
	
	GER_fix_return_dutch_east_indies = {
		## For some reason MY PUPPET that I thought for, just left me and joined the Australian faction???

		priority = 10

		allowed = {
			original_tag = GER
		}

		visible = {
			is_ai = no
			INS = {
				NOT = { is_puppet_of = GER }
			}
		}

		available = {
			INS = {
				is_in_faction_with = AST
				NOT = { is_in_faction_with = GER }
				NOT = { is_puppet_of = GER }
			}
		}
		
        fire_only_once = yes
		
        days_remove = 0
		
        modifier = {
        }
		
		complete_effect = {
			INS = {
				leave_faction = yes
			}
			GER = {
				puppet = INS
			}
		}
		
        remove_effect = {

        }
		
        cancel_trigger = {
        }
		
        cancel_effect = {
        }
    }
	
	GER_fix_alb_bul_war = {
		## Because MY communist puppet had the "proletariat revolution" idea from before, and it was MINE communist one because I am communist, it hit the >40% communism threshold... it started a communist civil war?????

		priority = 10

		allowed = {
			original_tag = GER
		}

		visible = {
			is_ai = no
			AND = {
				ALB = {
					has_war = yes 
				}
				BUL = {
					has_war = yes 
				}
			}
		}

		available = {
		}
		
        fire_only_once = yes
		
        days_remove = 0
		
        modifier = {
        }
		
		complete_effect = {
			every_country_with_original_tag = {
				original_tag_to_check = ALB
				limit = {
					is_puppet_of = GER
				}
				annex_country = {
					target = ALB
					transfer_troops = yes
				}
				add_autonomy_ratio = {
					value = -0.5
					localization = app_fixed_cw
				}
			}
			every_country_with_original_tag = {
				original_tag_to_check = BUL
				limit = {
					is_puppet_of = GER
				}
				annex_country = {
					target = BUL
					transfer_troops = yes
				}
				add_autonomy_score = {
					value = -500
					localization = app_fixed_cw
				}
			}
		}
		
        remove_effect = {

        }
		
        cancel_trigger = {
        }
		
        cancel_effect = {
        }
    }
	
}
