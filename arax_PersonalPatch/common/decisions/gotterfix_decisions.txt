GER_volkskommissariats_cat = {
	
	GER_gf_fix_baltics = {

		icon = GFX_decision_eng_trade_unions_support

		priority = 10

		allowed = {
			original_tag = GER
		}

		visible = {
			is_ai = no
		}

		available = {

		}

		cost = 5

		days_remove = 5

		ai_will_do = {
			base = 0
		}

		fire_only_once = yes

		targets = { EST LAT LIT }

		target_trigger = {
			FROM = {
				has_government = communism
				NOT = { is_subject_of = GER }
				NOT = { is_in_faction_with = GER }
			}
		}

		complete_effect = {

			puppet = {
				target = FROM
			}
			
		}

		remove_effect = {
			
			FROM = {
				country_event = {
					id = wuw_GER_diplomacy.71
					hours = 3
				}
			}

			event_option_tooltip = wuw_GER_diplomacy.71.a

		}
	}
	
	GER_gf_fix_venetia = {
		
		icon = GFX_decision_eng_trade_unions_support

		priority = 10

		allowed = {
			original_tag = GER
		}

		visible = {
			is_ai = no
		}

		available = {
			any_country_with_original_tag = {
				original_tag_to_check = ITA
				OR = {
					has_government = communism
					has_government = democratic
				}
				NOT = { has_war_with = GER }
			}
		}

		targets = { LBV }
		targets_dynamic = yes
		
		target_trigger = {
			FROM = {
				NOT = { is_subject_of = GER }
				is_subject_of = ITA
			}
		}
		
		cost = 25

		days_remove = 30

		ai_will_do = {
			base = 0
		}

		fire_only_once = yes

		modifier = {
			civilian_factory_use = 5
		}
		
		complete_effect = {
			ITA = {
				add_offsite_building = {
					type = industrial_complex
					level = 5
				}
			}
		}

		remove_effect = {
			
			random_country = {
				limit = {
					original_tag = LBV
					is_subject_of = GER
				}
				annex_country = {
					target = FROM
					transfer_troops = yes
				}
			}
			
			ITA = {
				add_offsite_building = {
					type = industrial_complex
					level = -5
				}
			}
		}
	}
	
	
	GER_gf_additional_kommissariat = {

		icon = GFX_decision_eng_trade_unions_support

		priority = 10

		allowed = {
			original_tag = GER
		}

		visible = {
			is_ai = no
		}

		available = {

		}

		cost = 25

		days_remove = 10

		ai_will_do = {
			base = 0
		}

		fire_only_once = yes

		targets = { LBV UKR EST }

		target_trigger = {
			FROM = {
				has_government = communism
				is_subject_of = GER
				NOT = { has_autonomy_state = autonomy_volkskommissariat }
			}
		}

		complete_effect = {

		}

		remove_effect = {
			
			FROM = {
				country_event = {
					id = wuw_GER_diplomacy.71
					hours = 3
				}
			}

			event_option_tooltip = wuw_GER_diplomacy.71.a

		}
	}
}